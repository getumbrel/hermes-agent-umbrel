services:
  # Web terminal UI + dashboard (proxied through Node server)
  web:
    build: .
    image: hermes-agent-umbrel:v2026.6.5
    container_name: hermes-web
    hostname: hermes # Shell prompt shows "hermes@hermes" instead of "hermes@<container-id>"
    restart: unless-stopped
    init: true
    user: "1000:1000"
    environment:
      APP_SEED: ${APP_SEED:-dev} # Provided by umbrelOS; fallback for local dev
      # Tells the dashboard to probe the gateway via HTTP instead of local PID detection
      GATEWAY_HEALTH_URL: http://hermes-gateway:8642
    ports:
      # Expose local dev on 18790 to match the Umbrel app port; the Node server listens on 18789.
      - "18790:18789"
    volumes:
      - ./data:/opt/data
    networks:
      - hermes-net
    depends_on:
      - gateway

  # Hermes messaging gateway
  gateway:
    image: hermes-agent-umbrel:v2026.6.5
    container_name: hermes-gateway
    restart: unless-stopped
    entrypoint: ["/init"]
    # Umbrel/Docker already supervises this dedicated gateway container, so opt out
    # of Hermes' internal s6 gateway supervision and run the gateway in foreground.
    command: ["/opt/hermes/docker/main-wrapper.sh", "gateway", "run", "--no-supervise"]
    environment:
      HERMES_UID: "1000"
      HERMES_GID: "1000"
      # API server exposes the health endpoint the dashboard probes via GATEWAY_HEALTH_URL.
      # Must bind to 0.0.0.0 (not default 127.0.0.1) to be reachable from the web container.
      # Binding to 0.0.0.0 requires an API key — we reuse APP_SEED since the port is
      # internal only (not exposed to host).
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_KEY: ${APP_SEED:-hermes-internal-key}
    volumes:
      - ./data:/opt/data
    networks:
      - hermes-net

networks:
  hermes-net:
    driver: bridge
